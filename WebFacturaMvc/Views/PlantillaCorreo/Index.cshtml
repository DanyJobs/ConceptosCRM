@model IEnumerable<WebFacturaMvc.Datos.PlantillaCorreo>

@{
   
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescpPlantilla)
            </th>
        
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescpPlantilla)
            </td>
        
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idPlantilla }) |
                @Html.ActionLink("Details", "Details", new { id=item.idPlantilla }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idPlantilla })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
